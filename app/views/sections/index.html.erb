<style>
body {
  background: linear-gradient(
    to bottom,
    #004a99 10%,
    rgba(0, 74, 153, 0.5) 70%,
    rgba(0, 74, 153, 0) 100%
  );
  min-height: 100vh;
}
  .section-glass-card {
    background: rgba(255, 255, 255, 0.25);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.4);
    border-radius: 20px;
    transition: all 0.25s ease;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
    overflow: hidden;
  }

  .section-glass-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  }

  .section-glass-body {
    background: rgba(13, 110, 253, 0.1);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.2);
    color: #052659;
    height: 150px;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: 1rem 1.25rem;
  }

  .section-glass-body h5 {
    font-weight: 700;
    margin-bottom: 0;
  }

  .section-glass-actions {
    background: rgba(255, 255, 255, 0.5);
    backdrop-filter: blur(6px);
    border-top: 1px solid rgba(0, 0, 0, 0.05);
  }

  .section-glass-actions i {
    color: #333;
    transition: color 0.18s ease;
  }

  .section-glass-actions a:nth-child(1):hover i,
  .section-glass-actions a:nth-child(1):focus-visible i {
    color: #0d6efd;
  }

  .section-glass-actions a:nth-child(2):hover i,
  .section-glass-actions a:nth-child(2):focus-visible i {
    color: #6c757d;
  }

  .section-glass-actions a:nth-child(3):hover i,
  .section-glass-actions a:nth-child(3):focus-visible i {
    color: #dc3545;
  }

  .btn-new-section {
    background: #198754;
    color: white;
    border: none;
    border-radius: 50px;
    padding: 0.6rem 1.4rem;
    font-weight: 600;
    box-shadow: 0 3px 10px rgba(25, 135, 84, 0.4);
    transition: all 0.2s ease;
  }

  .btn-new-section:hover {
    background: #157347;
    transform: scale(1.03);
    color: white;
  }
</style>

<div class="d-flex justify-content-between align-items-center mb-4">
  <div class="d-flex align-items-center gap-2">
    <i class="bi bi-list-check  fs-3"></i>
    <h2 class="fw-bold m-0">List of Sections</h2>
  </div>

  <%= link_to new_section_path, class: "btn btn-new-section d-flex align-items-center gap-2" do %>
    <i class="bi bi-clipboard2-plus-fill"></i>
    <span>New Section</span>
  <% end %>
</div>

<div class="row g-4">
  <% @sections.each do |section| %>
    <div class="col-12 col-sm-6 col-md-4 col-lg-3">
      <div class="section-glass-card h-100">
        <div class="section-glass-body">
          <div class="d-flex justify-content-between align-items-start">
            <i class="bi bi-diagram-3-fill fs-2 opacity-75"></i>
          </div>
          <div>
            <h5 class="text-truncate"><%= section.name %></h5>
          </div>
        </div>

        <div class="section-glass-actions d-flex justify-content-end align-items-center gap-4 p-2 pe-3">
          <%= link_to section, class: "fs-5" do %>
            <i class="bi bi-eye"></i>
          <% end %>

          <%= link_to edit_section_path(section), class: "fs-5" do %>
            <i class="bi bi-pencil"></i>
          <% end %>

          <%= link_to "#",
              class: "fs-5",
              data: { 
                bs_toggle: "modal",
                bs_target: "#confirmDeleteModal",
                section_id: section.id 
              } do %>
            <i class="bi bi-trash"></i>
          <% end %>
        </div>
      </div>
    </div>
  <% end %>
</div>

<!-- Delete Confirmation Modal -->
<div class="modal fade" id="confirmDeleteModal" tabindex="-1" aria-labelledby="confirmDeleteLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header bg-danger text-white">
        <h5 class="modal-title" id="confirmDeleteLabel">Confirm Delete</h5>
        <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
      </div>
      <div class="modal-body">
        Are you sure you want to delete this section?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
        <a href="#" id="confirmDeleteBtn" class="btn btn-danger">Delete</a>
      </div>
    </div>
  </div>
</div>